<Page x:Class="Sem3_kurs.OwnersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Владельцы">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Владельцы недвижимости" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"/>

            <Button Content="➕ Добавить владельца" 
                    Margin="20,0,0,0" 
                    Padding="10,5"
                    Click="BtnAddOwner_Click"
                    Background="#2ecc71"
                    Foreground="White"/>
        </StackPanel>

        <!-- Таблица владельцев -->
        <DataGrid Grid.Row="1" 
                  x:Name="OwnersDataGrid" 
                  Margin="10"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="True"
                  ItemsSource="{Binding}"
                  PreviewKeyDown="OwnersDataGrid_PreviewKeyDown"
                  CellEditEnding="OwnersDataGrid_CellEditEnding">

            <DataGrid.Columns>
                <!-- ФИО -->
                <DataGridTextColumn Header="ФИО" 
                                    Binding="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"
                                    Width="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Телефон -->
                <DataGridTextColumn Header="Телефон" 
                                    Binding="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                                    Width="120"/>

                <!-- Email -->
                <DataGridTextColumn Header="Email" 
                                    Binding="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                    Width="150"/>

                <!-- Адрес -->
                <DataGridTextColumn Header="Адрес" 
                                    Binding="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                                    Width="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Количество объектов -->
                <DataGridTextColumn Header="Объектов" 
                                    Binding="{Binding Properties.Count}"
                                    Width="80"
                                    IsReadOnly="True"/>
            </DataGrid.Columns>

            <!-- Подсветка строк с ошибками -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsValid}" Value="False">
                            <Setter Property="Background" Value="#FFF0F0"/>
                            <Setter Property="ToolTip" Value="Исправьте ошибки в данных"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <!-- Подсказка для пользователя -->
            <DataGrid.ToolTip>
                <TextBlock>
                    <Run Text="• Двойной клик по ячейке - редактирование"/>
                    <LineBreak/>
                    <Run Text="• Выделите строку и нажмите Delete для удаления"/>
                </TextBlock>
            </DataGrid.ToolTip>
        </DataGrid>
    </Grid>
</Page>