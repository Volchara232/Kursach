<Page x:Class="Sem3_kurs.OwnersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Владельцы">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Владельцы недвижимости" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"/>

            <Button Content="Добавить владельца" 
                    Margin="20,0,0,0" 
                    Padding="10,5"
                    Click="BtnAddOwner_Click"/>
        </StackPanel>

        <!-- Список владельцев -->
        <ListView Grid.Row="1" 
                  x:Name="OwnersListView" 
                  Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ФИО" 
                                    DisplayMemberBinding="{Binding FullName}" 
                                    Width="200"/>
                    <GridViewColumn Header="Телефон" 
                                    DisplayMemberBinding="{Binding Phone}" 
                                    Width="120"/>
                    <GridViewColumn Header="Email" 
                                    DisplayMemberBinding="{Binding Email}" 
                                    Width="150"/>
                    <GridViewColumn Header="Объектов" 
                                    DisplayMemberBinding="{Binding PropertiesCount}" 
                                    Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>