<Window x:Class="Sem3_kurs.AddPropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:enums="clr-namespace:Sem3_kurs.Enums"
        Title="Добавление объекта недвижимости"
        Height="650"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <ObjectDataProvider x:Key="DealTypes" 
                            MethodName="GetValues" 
                            ObjectType="{x:Type enums:DealType}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:DealType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Добавление объекта недвижимости" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        <!-- Форма -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="Основная информация">
                    <StackPanel Margin="5">
                        <TextBlock Text="Тип объекта*" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtType" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Адрес*" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtAddress" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Район" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtDistrict" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Тип сделки*" Margin="0,0,0,2"/>
                        <ComboBox x:Name="CmbDealType" 
                                  ItemsSource="{Binding Source={StaticResource DealTypes}}"
                                  Height="25"
                                  Margin="0,0,0,10"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Характеристики" Margin="0,10,0,0">
                    <StackPanel Margin="5">
                        <TextBlock Text="Количество комнат*" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtRooms" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Общая площадь (м²)*" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtTotalArea" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Жилая площадь (м²)" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtLivingArea" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Площадь кухни (м²)" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtKitchenArea" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Этаж*" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtFloor" 
                                 Height="25"
                                 Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <CheckBox x:Name="ChkHasBalcony" 
                                      Content="Балкон" 
                                      Margin="0,0,20,0"/>
                            <CheckBox x:Name="ChkHasLoggia" 
                                      Content="Лоджия"/>
                        </StackPanel>

                        <TextBlock Text="Цена (руб.)*" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtPrice" 
                                 Height="25"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Владелец" Margin="0,10,0,0">
                    <StackPanel Margin="5">
                        <TextBlock Text="Выберите владельца*" Margin="0,0,0,2"/>
                        <ComboBox x:Name="CmbOwner" 
                                  DisplayMemberPath="FullName"
                                  Height="25"/>
                    </StackPanel>
                </GroupBox>

                <!-- Описание -->
                <GroupBox Header="Дополнительная информация" Margin="0,10,0,0">
                    <StackPanel Margin="5">
                        <TextBlock Text="Описание дома" Margin="0,0,0,2"/>
                        <TextBox x:Name="TxtDescription" 
                                 Height="60"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,10"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Content="Отмена" 
                    Width="80" 
                    Height="30"
                    Margin="0,0,10,0"
                    Click="BtnCancel_Click"/>

            <Button Content="Сохранить" 
                    Width="80" 
                    Height="30"
                    IsDefault="True"
                    Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>