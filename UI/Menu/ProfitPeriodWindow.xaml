<Window x:Class="Sem3_kurs.ProfitPeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Расчет прибыли за период"
        Height="200"
        Width="350"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Выберите период для расчета прибыли"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="С:" Width="40" VerticalAlignment="Center"/>
            <DatePicker x:Name="DpStartDate" Width="120" Margin="5,0,20,0"/>

            <TextBlock Text="По:" Width="40" VerticalAlignment="Center"/>
            <DatePicker x:Name="DpEndDate" Width="120" Margin="5,0,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="2" x:Name="TxtPreview" 
                   Margin="0,0,0,10"
                   Foreground="Gray"/>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Отмена" 
                    Width="80" 
                    Height="30"
                    Margin="0,0,10,0"
                    Click="BtnCancel_Click"/>

            <Button Content="Рассчитать" 
                    Width="100" 
                    Height="30"
                    IsDefault="True"
                    Click="BtnCalculate_Click"/>
        </StackPanel>
    </Grid>
</Window>